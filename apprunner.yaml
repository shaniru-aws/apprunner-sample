version: 1.0
runtime: nodejs12
build:
  commands:    
    build:
      - npm install                         
run:                              
  command: node index.js
  network:
    port: 3000
    env: APP_PORT
